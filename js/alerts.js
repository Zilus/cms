/*** Dialogos ***/
function Confirm(toUrl)
{
      if(confirm("Estas seguro de querer Eliminar el Registro \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function QuiConfirm(toUrl)
{
      if(confirm("Estas seguro de hacer tu Quiniela \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function ConfirmCerrar(toUrl)
{
      if(confirm("Estas seguro de querer Terminar el Registro \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function Activar(toUrl)
{
      if(confirm("Estas seguro de querer Activar el Registro \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function QuoteAccept(toUrl)
{
      if(confirm("Estas seguro de querer Aceptar la cotización \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function ConfirmAddFriend(toUrl)
{
      if(confirm("Estas seguro de querer agregar este amigo \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function ConfirmMsgDel(toUrl)
{
      if(confirm("Estas seguro de querer eliminar el mensaje \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function ConfirmMMsgDel(toUrl)
{
      if(confirm("Estas seguro de querer eliminar los mensajes seleccionados \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function ConfirmDelFriend(toUrl)
{
      if(confirm("Estas seguro de querer eliminar a tu amigo \n"
                   +"Click OK para continuar o Cancel para abortar."))
             return true;
      else
             return false;
}
function NotYetWarn(toUrl)
{
      if(alert("Los Resultados los podrás ver una vez \n"
	  	+ "que arranque el primer partido de la jornada"))
             return true;
      else
             return false;
}